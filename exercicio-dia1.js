const nota = 100;

if (nota => 80)
    console.log('Passou Viado!')

else if(nota < 80 || nota >= 60)
    console.log('iiiiiiii, Ta na lista de espera')

else if(nota > 60)
    console.log('Já era viado, só ano que vem')
